import { GoogleChartDirective } from './google-chart.directive';

describe('GoogleChartDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleChartDirective();
    expect(directive).toBeTruthy();
  });
});
